name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    name: Test on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        bazel-version: ["7.*", "8.*", "9.*"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Bazel
        uses: bazel-contrib/setup-bazel@0.9.0
        with:
          bazelisk-version: "1.x"
          bazelisk-cache: true
          disk-cache: ${{ github.workflow }}
          repository-cache: true

      - name: Build all targets
        run: bazel build //...

      - name: Run all tests
        run: bazel test //verilator/tests:adder_test //verilator/tests:include_top_test //verilator/tests:load_and_count_test //verilator/tests:nested_module_test --test_output=errors

      - name: Run trace tests
        run: bazel test //verilator/tests:adder_trace_test --test_output=errors

      - name: Run hierarchical tests
        run: bazel test //verilator/tests:hierarchical_compile_test --test_output=errors

      - name: Build with SystemC support
        run: bazel build --config=systemc //verilator/tests:adder_systemc_verilator

      - name: Run SystemC tests (optional)
        run: bazel test --config=systemc //verilator/tests:adder_systemc_test --test_output=errors || true

      - name: Check formatting
        run: |
          bazel run @rules_license//tools:check_license_headers || true
